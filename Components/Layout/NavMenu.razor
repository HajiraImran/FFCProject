<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">FFC Login App</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="weather">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Weather
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="register">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Register
            </NavLink>
        </div>
    </nav>
</div>

    {
        background: linear-gradient(to bottom, #1a1a1a, #2e2e2e) !important;
    }
        }
<style>
    .nav-scrollable {
        background: linear-gradient(to bottom, #2c2c2c, #0f0f0f); /* dark gray gradient */
        color: white;
        min-height: 100vh;
        width: 250px;
        padding-top: 20px;
        padding-left: 10px;
        padding-right: 10px;
        box-shadow: inset -2px 0 5px rgba(0, 0, 0, 0.6); /* subtle inner shadow */
    }

    .nav-link {
        color: white;
        text-decoration: none;
        font-weight: 500;
        padding: 10px;
        display: block;
        border-radius: 4px;
        transition: background-color 0.3s ease, color 0.3s ease;
    }

        .nav-link:hover,
        .nav-link.active {
            background-color: #444;
            color: #00ffff; /* neon cyan effect */
        }

    .nav-item {
        margin-bottom: 10px;
    }
</style>


